import 'package:flutter/material.dart';

class AdminAssignmentList extends StatelessWidget {
  const AdminAssignmentList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Admin Portal'),
    );
  }
}
